/**
 * Interactive Git remote setup flow.
 * Used by both `claude-sync init` (when user opts in) and `claude-sync sync setup`.
 */
export declare function setupGitSync(claudeSyncDir: string, urlArg?: string): Promise<void>;
//# sourceMappingURL=sync-setup.d.ts.map